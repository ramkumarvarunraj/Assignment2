apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-app-svc
spec:
  type: NodePort
  selector:
    app: aceest-fitness-app
    color: green # Initially, point to the green deployment
  ports:
  - port: 80
    targetPort: 8080
